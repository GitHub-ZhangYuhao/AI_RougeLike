interface:
  display_name: "视频转透明序列帧"
  short_description: "将视频一键转为透明 PNG 序列和可配置 Flipbook 图集"
  default_prompt: "Use $video-to-alpha-flipbook to convert this video into an alpha PNG sequence and a game-ready sprite sheet."
policy:
  allow_implicit_invocation: true
